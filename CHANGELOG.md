# Changelog

## 0.1.0-alpha.1

- Field Manual (`book/manuscript.md`, built to PDF/DOCX in `dist/`)
- Executable Claude Code Rules, Skills, and a read-only-enforced Agent (`.claude/`)
- TaskFlow sandbox (`examples/taskflow/`), 8/8 tests passing
- Deterministic architecture checker (`tools/architecture-checker/`)
- Friction Ledger with real, dated entries from this project's own history
- Preregistered evaluation protocol — frozen tasks, held-out acceptance tests, a grading rubric, a machine-readable contract (`evals/`); the 18-trial pilot itself has not been run
- 10-minute walkthrough (`docs/10-minute-metavibe.md`)

No empirical performance-uplift claim is made in this release. See `book/manuscript.md`'s Status & Evidence section.
